# ============================================
# PLANTAE - Variáveis de Ambiente (Produção)
# ============================================
# Copie este arquivo e preencha os valores nas plataformas

# ============ RENDER (Backend) ============
NODE_ENV=production
PORT=10000

# ============ NEON (PostgreSQL) ============
# Pegue a connection string no dashboard do Neon
# Formato: postgresql://usuario:senha@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@ep-xxxxx.us-east-1.aws.neon.tech/plantae_db?sslmode=require

# ============ UPSTASH (Redis) ============
# Pegue a URL no dashboard do Upstash
# Formato: rediss://default:senha@host:porta
REDIS_URL=rediss://default:xxxxx@us1-xxxxx.upstash.io:6379

# ============ JWT ============
JWT_SECRET=sua_chave_secreta_muito_segura_aqui_minimo_32_caracteres
JWT_EXPIRES_IN=7d

# ============ CORS ============
# URLs permitidas (separadas por vírgula se várias)
CORS_ORIGIN=*
